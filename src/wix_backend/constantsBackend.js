/* Constants used in backend */

// TODO: Update api_id, region, stage, and api_name in the url below
export const ENVDATA_API_URL = `https://${api_id}.execute-api.${region}.amazonaws.com/${stage}/${api_name}`;